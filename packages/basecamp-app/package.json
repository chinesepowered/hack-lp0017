{
  "name": "@whistleblower/basecamp-app",
  "version": "0.1.0",
  "description": "Whistleblower Basecamp app — upload, broadcast, and anchor documents on the Logos stack",
  "license": "(MIT OR Apache-2.0)",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no unit tests; e2e via tests/integration' && exit 0"
  },
  "dependencies": {
    "@whistleblower/indexing-module": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "@types/node": "^20.11.0"
  }
}
