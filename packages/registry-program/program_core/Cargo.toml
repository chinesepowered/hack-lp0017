[package]
name = "whistleblower_registry_core"
description = "Shared types for the Whistleblower registry SPEL program"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
borsh = "1.5"
