{
  "name": "whistleblower",
  "version": "0.1.0",
  "private": true,
  "description": "Whistleblower — censorship-resistant document upload and indexing for the Logos stack",
  "license": "(MIT OR Apache-2.0)",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "demo": "bash scripts/demo.sh",
    "benchmark": "bash scripts/benchmark.sh"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0",
    "vitest": "^1.4.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.22.0"
}
